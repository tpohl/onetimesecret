{{>header}}

  {{^actionable_visitor}}
  <div class="jumbotron">
    <h2 class="intro">Return to <a href="{{subdomain.homepage}}">{{subdomain.company_domain}}</a></h2>
  </div>
  {{/actionable_visitor}}

  {{#actionable_visitor}}
  <div class="jumbotron">

    {{^authenticated}}
    <h1 class="">{{i18n.page.tagline1}}</h1>
    <p class="lead">{{i18n.page.tagline2}}</p>
    {{/authenticated}}

    {{>partial/create_secret_form}}

    {{^authenticated}}
    <hr/>
    <p class="hint">{{i18n.page.secret_hint}}</p>
    {{/authenticated}}
    <hr/>

    <div class="alert alert-info">
      <button type="button" class="close" data-dismiss="alert">&times;</button>
      {{i18n.page.get_ots0}} <a href="https://github.com/siw36/onetimesecret">{{i18n.page.get_ots1}}</a>
    </div>

  </div>
  {{/actionable_visitor}}

{{>footer}}
